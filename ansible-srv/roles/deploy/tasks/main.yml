---
- name: Putting variables file 
  template:
    src: ./variables.yml
    dest: /web-srv/variables.yml

- name: Putting sample file 
  template:
    src: ./templates/sample.yml
    dest: /web-srv/

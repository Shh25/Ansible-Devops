name: web-srv-4
vm:
  ip: 192.168.33.104
  memory: 2048
config:
  - template: 
      src: ../ansible-srv/web-srv.pub 
      dest: ~/.ssh/public_key

commands:
  serve: cat ~/.ssh/public_key >> ~/.ssh/authorized_keys

  
